Упростить форму создания/редактирования записи в AddEntryView.


## Функциональные требования

### Изменения в AddEntryView:

- оставить только поля Title и Notes, удалив дополнительные поля (Year, Genre, Author, Platform)
- изменить отображение в ContentView: показывать первую строку из notes вместо набора additionalFields.

1. **Изменить заголовок формы**
   - Кнопка Cancel: вместо надписи сделать иконку "назад";
   - Кнопка Save: вместо надписи сделать иконку "галочка" и сделать кнопку синей (следуй стандартам apple);
   - Надпись New Entry / Edit Entry убрать; 

2. **Убрать отдельные поля с выбором даты (Date) и оценки (Score)**
   - Убреть поля Score, Date из списка полей на экране;
   - Перенести эти поля в toolbar в заголовке экрана, слева от Save (синяя кнопка с галочкой);
   - Должно появиться 2 кнопки с рейтингом (звездочка) и календарем;
   
3. **Поведение рейтинга и календаря**
   - При нажатии на рейтинг должно появляться меню с выбором нужного score по старой системе;
   - При нажатии на календарь — datepicker внизу экрана

4. **Удалить дополнительные поля ввода**:
   - Удалить секцию `additionalFieldsSection` с полями: Year, Genre, Author, Platform
   - Удалить DynamicFieldInput компоненты
   - Оставить только: Collection picker, Title, Notes, Images

5. **Улучшить поле Notes**:
   - Сделать поле многострочным с увеличенным размером
   - `lineLimit(6...10)` - минимум 6 строк, максимум 10
   - Поддержка авторасширения по мере ввода текста
   - Placeholder: "Your thoughts, details, year, genre..."

6. **Миграция существующих данных**:
   - При загрузке записи с заполненными additionalFields (год, жанр и т.д.):
     - Перенести данные из additionalFields в начало notes
     - Формат: "Year: 2023, Genre: Action\n[существующее описание]"
     - После переноса additionalFields можно очистить (опционально)
   - Выполнить миграцию при первой загрузке записи в AddEntryView

4. **Изменения в AI помощнике**:
   - AI помощник теперь заполняет notes следующим образом:
     - Первая строка: метаданные (год, жанр, автор, платформа) в формате "Year: 2023, Genre: Action"
     - Со второй строки: описание из API
   - Пример результата:
     ```
     2023, Netflix, Action, Adventure
     An epic story about...
     ```

### Изменения в ContentView:

1. **List view (EntryListRow)**:
   - Вместо отображения additionalFields (год, жанр) показывать первую строку из notes
   - Использовать `.lineLimit(1)` для автоматической обрезки
   - Если notes пустое - показывать placeholder "(no notes)"
   - Цвет текста: `.secondary`

2. **Grid view (EntryCard)** - без изменений:
   - В grid view notes не показываются, только title и cover
   - Оставить как есть

## Визуальные требования

### AddEntryView - Notes section:
- **Расположение**: После секции Title, перед Images
- **Заголовок**: "Notes" (как сейчас)
- **Размер поля**:
  - Минимум 6 строк видимых
  - Максимум 10 строк с авторасширением
  - При большем количестве текста - скролл внутри поля
- **Стиль**:
  - Тот же дизайн что и сейчас (rounded rectangle, systemGray6 background)
  - Padding 12pt
- **Placeholder**: "Your thoughts, details, year, genre..."

### ContentView - List view:
- **Вторая строка списка**:
  - Текущее: "Year, Genre, Author, Platform" из additionalFields
  - Новое: Первая строка из notes с `.lineLimit(1)`
- **Пример**:
  ```
  📚 The Great Gatsby                [название, жирным]
  A classic American novel set in... [первая строка notes, обычным]
  ```
- **Если notes пустое**: показать "(no notes)" серым цветом

## Важное
- Не забудь про Definition of Done в ./CLAUDE.md

## Что было реализовано

### Изменения в AddEntryView

✅ **Изменен заголовок формы**:
- Кнопка Cancel: заменена на иконку "chevron.left" (назад)
- Кнопка Save: заменена на иконку "checkmark" с синим цветом (.tint(.blue))
- Надпись "New Entry / Edit Entry" удалена (убран navigationTitle)

✅ **Перенесены Score и Date в toolbar**:
- Удалены отдельные секции scoreSection и dateSection из body
- Score: добавлена кнопка с Menu в toolbar (звездочка + emoji текущего рейтинга)
- Date: добавлена кнопка с календарем в toolbar, открывает DatePickerSheet
- Обе кнопки размещены в .principal toolbar placement между кнопками назад и сохранить

✅ **Удалены дополнительные поля**:
- Удалена секция additionalFieldsSection (Year, Genre, Author, Platform)
- Удален struct DynamicFieldInput (строки ~460-494)
- Удалена переменная @State fieldValues
- Удален метод binding(for:)

✅ **Улучшено поле Notes**:
- Увеличен lineLimit до 6...10 (было 3...6)
- Изменен placeholder: "Your thoughts, details, year, genre..."
- Поле теперь занимает больше места и автоматически расширяется

✅ **Миграция существующих данных**:
- Реализован метод миграции в loadItemData(_ item:)
- При загрузке записи с additionalFields:
  - Если notes пустое: переносятся только поля
  - Если notes есть: поля добавляются в начало с переносом строки
  - Формат: "Author: John, Genre: Drama, Year: 2023"
- При сохранении additionalFields очищается (устанавливается в [:])

✅ **Изменения в AI помощнике**:
- Метод applyOptionToEntry изменен для нового формата
- Первая строка: метаданные через запятую (values из additionalFields)
- Вторая строка и далее: описание из API
- Пример результата: "2023, Action, Adventure\nAn epic story about..."

✅ **Добавлен DatePickerSheet**:
- Новый компонент для выбора даты (строки ~653-679)
- Использует .graphical style datepicker
- Презентуется через .sheet с .presentationDetents([.height(400)])
- Кнопка "Done" для закрытия

### Изменения в ContentView

✅ **List view (EntryListRow)**:
- Изменен computed property metadataLine
- Теперь показывает первую строку из notes (до первого \n)
- Если notes пустое: показывает "(no notes)"
- Использует .lineLimit(1) для автоматической обрезки
- Цвет текста: .secondary (как было)

### Технические детали

**Файлы изменены**:
- `livlogios/Views/AddEntryView.swift`:
  - Удалено ~100 строк (секции scoreSection, dateSection, additionalFieldsSection, DynamicFieldInput)
  - Добавлено ~50 строк (новый toolbar, DatePickerSheet, изменения в логике)
  - Изменены методы: loadItemData, applyOptionToEntry, saveEntry

- `livlogios/Views/ContentView.swift`:
  - Изменен метод metadataLine в EntryListRow (2 вхождения - в EntryListRow и EntryCard)
  - Теперь показывает первую строку notes вместо additionalFields

**State переменные**:
- Удалена: `@State private var fieldValues: [String: String] = [:]`
- Добавлена: `@State private var showDatePicker = false`

**Обратная совместимость**:
- Существующие записи с additionalFields не теряют данные
- При первом редактировании данные мигрируются в notes
- additionalFields остается в Item модели, но всегда сохраняется как пустой словарь

### Definition of Done

✅ **SwiftLint**: Ошибки не связаны с изменениями (старые ошибки в именах типов и длине строк)
✅ **Build**: Проект успешно собран (BUILD SUCCEEDED)
✅ **Tests**: Все тесты прошли (TEST SUCCEEDED)

### Итог

Форма создания/редактирования записи упрощена:
- Только Title и Notes для пользовательского ввода
- Score и Date перенесены в компактный toolbar
- AI помощник заполняет notes в новом формате (метаданные + описание)
- List view показывает первую строку notes вместо additionalFields
- Миграция данных происходит автоматически при редактировании старых записей
