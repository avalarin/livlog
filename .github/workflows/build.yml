name: Build

on:
  push:
    branches:
      - '**'
    paths:
      - 'backend/**'
      - '.github/workflows/build.yml'
    # Tag pushes ignore the paths filter (GitHub Actions behavior)
    tags:
      - 'v*'

jobs:
  lint-and-test:
    name: Lint and Test
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache-dependency-path: backend/go.sum

      - name: Run linter
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.1
          working-directory: backend

      - name: Run tests
        working-directory: backend
        run: go test -race ./...

  mcp-eval:
    name: MCP Eval
    needs: lint-and-test
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' && !startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check for MCP file changes
        id: changes
        run: |
          if git diff --name-only HEAD~1 HEAD | grep -qE '^backend/internal/(handler/mcp_|service/mcp_|repository/mcp_)'; then
            echo "mcp=true" >> "$GITHUB_OUTPUT"
          else
            echo "mcp=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Go
        if: steps.changes.outputs.mcp == 'true'
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache-dependency-path: backend/go.sum

      - name: Run MCP eval
        if: steps.changes.outputs.mcp == 'true'
        working-directory: backend
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: go test -tags eval -v -run TestMCP_Eval ./internal/handler/ -timeout 5m -count=1

  push-image:
    name: Push Docker Image
    needs: lint-and-test
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Read version
        id: version
        working-directory: backend
        run: |
          VERSION=$(cat VERSION | tr -d '[:space:]')
          COMMIT=$(git rev-parse --short HEAD)
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "commit=${COMMIT}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/livlog
          tags: |
            type=raw,value=${{ steps.version.outputs.version }}-${{ steps.version.outputs.commit }},enable=${{ github.ref_type == 'branch' }}
            type=raw,value=latest,enable=${{ github.ref_type == 'tag' }}
            type=ref,event=tag

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            APP_VERSION=${{ steps.version.outputs.version }}
            APP_COMMIT=${{ github.ref_type == 'branch' && steps.version.outputs.commit || '' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
